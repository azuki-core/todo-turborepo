{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:tsc": "yarn build",
    "dev": "tsx watch --inspect=9229 server",
    "generate": "prisma generate && tsx --tsconfig tsconfig.json graphql/schema"
  },
  "dependencies": {
    "@apollo/server": "4.11.2",
    "@prisma/client": "6.2.0",
    "graphql": "^16.10.0",
    "nexus": "^1.3.0",
    "prisma": "^6.2.0"
  },
  "devDependencies": {
    "eslint": "9.17.0",
    "tsx": "4.19.2",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": "20.12.1"
  }
}
