schema: './src/generated/schema.graphql'
documents: ['./src/graphql/**/*.graphql']
generates:
  ./src/generated/apollo.ts:
    config:
      scalars:
        DateTime: string
    plugins:
      - 'typescript'
      - 'typed-document-node'
